{% extends "base.jinja2" %}

{% block content %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-6">
                <div class="text-center mt-4">
                    <img class="mb-4 img-error" src="{{ static("assets/img/error-404-monochrome.svg") }}" alt="404 image" />
                    <p class="lead">This requested URL was not found on this server.</p>
                    <a href="{{ url("index") }}">
                        <i class="fas fa-arrow-left me-1"></i>
                        Return to Dashboard
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
