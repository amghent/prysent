<nav class="sb-sidenav accordion sb-sidenav-dark" id="sidenavAccordion">
    <div class="sb-sidenav-menu">
        {% if user != None %}
            <div class="nav">
                {% for dashboard in dashboards %}
                    {% set __menu = {
                        "title": dashboard.menu,
                        "menu_items": []
                    } %}

                    {{ sidebar_factory.get_menu(__menu) }}
                {% endfor %}
            </div>
        {% endif %}
    </div>

    {% include "includes/user.jinja2" %}
</nav>
